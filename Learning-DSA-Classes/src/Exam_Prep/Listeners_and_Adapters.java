package Exam_Prep;

//Q : : What are the uses of listeners and adapters in the event handling process?
public class Listeners_and_Adapters  {
    /*
     public static void main(String[] args) {
        Frame f = new Frame("Listener & Adapter Demo");
        Button b = new Button("Click Me");
        b.setBounds(100, 100, 100, 50);
        f.add(b);

        // Using ActionListener (Listener)
        b.addActionListener(new ActionListener() {
            public void actionPerformed(ActionEvent e) {
                System.out.println("Button Clicked!");
            }
        });

        // Using MouseAdapter (Adapter)
        f.addMouseListener(new MouseAdapter() {
            public void mouseClicked(MouseEvent e) {
                System.out.println("Mouse Clicked at: " + e.getX() + ", " + e.getY());
            }
        });

        f.setSize(400, 300);
        f.setLayout(null);
        f.setVisible(true);
    }
*/
}
